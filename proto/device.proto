syntax = "proto3";
package openiot;

message StatusReport {
    uint64 device_id = 1;     // Unique Device ID
    uint32 message_size = 2;  // Actual, decoded message size
    bytes details = 3;        // Status details - device specific, maybe encrypted
}
